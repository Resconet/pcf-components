import { ContextFeatures } from "@resconet/workflow";

// TODO add features
// TODO this should probably be implemented in some common lib (power-workflow? or power-ui?)
export function getPowerWorkflowFeatures(): ContextFeatures {
	return new ContextFeatures();
}
